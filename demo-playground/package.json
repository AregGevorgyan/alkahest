{
  "name": "alkahest-demo-playground",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "start": "pnpm --filter cli run start",
    "dev": "concurrently \"pnpm --filter web dev\" \"pnpm --filter server start\"",
    "build": "pnpm --filter web build",
    "record": "pnpm --filter cli run record",
    "demo": "pnpm --filter cli run demo"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
